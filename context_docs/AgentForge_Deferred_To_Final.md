# AgentForge Deferred Items (Post-Early Submission)

These items are intentionally deferred to the Sunday final milestone to protect
early-submission stability.

## Deferred Product/Platform Work

- Open-source extraction/publishing track (`ghostagent/*` package release workflow).
- Human-in-the-loop escalation workflow for high-risk responses.
- User feedback loop (thumbs up/down + correction capture + triage pipeline).

## Deferred Eval Framework Expansions

- Deterministic replay harness for consistency/regression stability.
- Rubric-based quality scoring with weighted dimensions.
- Controlled variant experiments for model/prompt/tool configuration comparison.
- Retrieval-specific metrics (if retrieval layer is expanded in future architecture).

## Deferred Operational Reporting

- Full AI cost analysis deliverable with:
  - measured development spend,
  - monthly projections at 100/1K/10K/100K users,
  - explicit assumptions and sensitivity ranges.
- Automated long-term eval trend dashboards (beyond JSON artifact history).

## Why Deferred

- Early submission priority is demonstrable progress in:
  - tooling breadth and correctness,
  - memory and verification systems,
  - eval/observability foundation.
- The deferred items are additive and non-blocking for showing strong trajectory
  against the PRD.
